\subsection*{The Integer Embedding}

\begin{definitionbox}{Definition}
\begin{definition}[Embedding of $\mathbb{Z}$ into $\mathbb{Q}$]
\label{def:rational-chapter-embedding-z-into-q}
Define
\[
\iota:\mathbb{Z}\to\mathbb{Q},
\qquad
\iota(n):=[n,1_{\mathbb{Z}}].
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
For every $n\in\mathbb{Z}$,
\[
\iota(n)=[n,1_{\mathbb{Z}}].
\]
\end{remark*}

\begin{remark*}[Interpretation]
The integer embedding sends an integer to the rational class with denominator
one.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:rationals]{Rational Numbers}
  \item \hyperref[def:one-in-z]{One in $\mathbb{Z}$}
  \item \hyperref[thm:zero-not-one-in-z]{Zero Is Not One in $\mathbb{Z}$}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[Embedding Preserves Zero]
\label{thm:embedding-z-into-q-preserves-zero}
The embedding sends integer zero to rational zero:
\[
\iota(0_{\mathbb{Z}})=0_{\mathbb{Q}}.
\]
\hyperref[prf:embedding-z-into-q-preserves-zero]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\iota(0_{\mathbb{Z}})=0_{\mathbb{Q}}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The embedding preserves the additive zero constant.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:rational-chapter-embedding-z-into-q]{Embedding of $\mathbb{Z}$ into $\mathbb{Q}$}
  \item \hyperref[def:zero-in-z]{Zero in $\mathbb{Z}$}
  \item \hyperref[def:zero-in-q]{Zero in $\mathbb{Q}$}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[Embedding Preserves One]
\label{thm:embedding-z-into-q-preserves-one}
The embedding sends integer one to rational one:
\[
\iota(1_{\mathbb{Z}})=1_{\mathbb{Q}}.
\]
\hyperref[prf:embedding-z-into-q-preserves-one]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\iota(1_{\mathbb{Z}})=1_{\mathbb{Q}}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The embedding preserves the multiplicative one constant.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:rational-chapter-embedding-z-into-q]{Embedding of $\mathbb{Z}$ into $\mathbb{Q}$}
  \item \hyperref[def:one-in-z]{One in $\mathbb{Z}$}
  \item \hyperref[def:one-in-q]{One in $\mathbb{Q}$}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[Embedding $\mathbb{Z}$ into $\mathbb{Q}$ Is Injective]
\label{thm:embedding-z-into-q-injective}
For all $m,n\in\mathbb{Z}$,
\[
\iota(m)=\iota(n)\Longrightarrow m=n.
\]
\hyperref[prf:embedding-z-into-q-injective]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
For all $m,n\in\mathbb{Z}$,
\[
\iota(m)=\iota(n)\Longrightarrow m=n.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Distinct integers remain distinct after embedding into the rationals.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[prop:equality-criterion-for-rational-classes]{Equality Criterion for Rational Classes}
  \item \hyperref[thm:one-multiplicative-identity-on-z]{One Is a Multiplicative Identity on $\mathbb{Z}$}
\end{itemize}
\end{dependencies}

\subsection*{Preservation of Algebra and Order}

\begin{theorembox}{Theorem}
\begin{theorem}[Embedding Preserves Addition]
\label{thm:embedding-z-into-q-preserves-addition}
For all $m,n\in\mathbb{Z}$,
\[
\iota(m+n)=\iota(m)+\iota(n).
\]
\hyperref[prf:embedding-z-into-q-preserves-addition]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
For all $m,n\in\mathbb{Z}$,
\[
\iota(m+n)=\iota(m)+\iota(n).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The embedding preserves addition.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  
\item \hyperref[def:rational-chapter-embedding-z-into-q]{Embedding $\mathbb{Z}$ into $\mathbb{Q}$}
  \item \hyperref[def:addition-on-z-classes]{Addition on $\mathbb{Z}$}
  \item \hyperref[def:addition-on-q-classes]{Addition on $\mathbb{Q}$}
  \item \hyperref[thm:addition-on-z-well-defined]{Addition on $\mathbb{Z}$ Is Well-Defined}
  \item \hyperref[thm:addition-on-q-well-defined]{Addition on $\mathbb{Q}$ Is Well-Defined}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[Embedding Preserves Multiplication]
\label{thm:embedding-z-into-q-preserves-multiplication}
For all $m,n\in\mathbb{Z}$,
\[
\iota(m\cdot n)=\iota(m)\cdot\iota(n).
\]
\hyperref[prf:embedding-z-into-q-preserves-multiplication]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
For all $m,n\in\mathbb{Z}$,
\[
\iota(m\cdot n)=\iota(m)\cdot\iota(n).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The embedding preserves multiplication.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:rational-chapter-embedding-z-into-q]{Embedding $\mathbb{Z}$ into $\mathbb{Q}$}
  \item \hyperref[def:multiplication-on-z-classes]{Multiplication on $\mathbb{Z}$}
  \item \hyperref[def:multiplication-on-q-classes]{Multiplication on $\mathbb{Q}$}
  \item \hyperref[thm:multiplication-on-z-well-defined]{Multiplication 
on $\mathbb{Z}$ Is Well-Defined}
  \item \hyperref[thm:multiplication-on-q-well-defined]{Multiplication on $\mathbb{Q}$ Is Well-Defined}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[Embedding Preserves Order]
\label{thm:embedding-z-into-q-preserves-order}
For all $m,n\in\mathbb{Z}$,
\[
m\le n
\Longleftrightarrow
\iota(m)\le\iota(n).
\]
\hyperref[prf:embedding-z-into-q-preserves-order]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
For all $m,n\in\mathbb{Z}$,
\[
m\le n
\Longleftrightarrow
\iota(m)\le\iota(n).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The embedding preserves and reflects the integer order inside the rational
order.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:addition-on-q-classes]{Addition on $\mathbb{Q}$-Classes}
  \item \hyperref[def:multiplication-on-q-classes]{Multiplication on $\mathbb{Q}$-Classes}
  \item \hyperref[def:order-on-q]{Order on $\mathbb{Q}$}
  \item \hyperref[thm:z-totally-ordered-set]{$\mathbb{Z}$ Is a Totally Ordered Set}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[$\mathbb{Z}$ Embeds into $\mathbb{Q}$]
\label{thm:z-embeds-into-q}
The map $\iota:\mathbb{Z}\to\mathbb{Q}$ is an injective order-preserving ring
homomorphism.
\hyperref[prf:z-embeds-into-q]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{Injective}(\iota)
\land
\operatorname{Homomorphism}(\iota,\mathbb{Z},\mathbb{Q},+)
\land
\operatorname{Homomorphism}(\iota,\mathbb{Z},\mathbb{Q},\cdot)
\land
\operatorname{OrderEmbedding}(\iota,\mathbb{Z},\mathbb{Q},\le).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The map from integers to rationals is an injective structure-preserving
embedding for the ring operations and order.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[thm:embedding-z-into-q-preserves-zero]{Embedding Preserves Zero}
  \item \hyperref[thm:embedding-z-into-q-preserves-one]{Embedding Preserves One}
  \item \hyperref[thm:embedding-z-into-q-injective]{Embedding $\mathbb{Z}$ into $\mathbb{Q}$ Is Injective}
  \item \hyperref[thm:embedding-z-into-q-preserves-addition]{Embedding Preserves Addition}
  \item \hyperref[thm:embedding-z-into-q-preserves-multiplication]{Embedding Preserves Multiplication}
  \item \hyperref[thm:embedding-z-into-q-preserves-order]{Embedding Preserves Order}
\end{itemize}
\end{dependencies}

\subsection*{Field of Fractions}

\begin{theorembox}{Theorem}
\begin{theorem}[$\mathbb{Q}$ Is the Field of Fractions of $\mathbb{Z}$]
\label{thm:q-is-field-of-fractions-of-z}
For every $x\in\mathbb{Q}$, there exist $m,n\in\mathbb{Z}$ with
$n\ne0_{\mathbb{Z}}$ such that
\[
x=\iota(m)\cdot \iota(n)^{-1}.
\]
\hyperref[prf:q-is-field-of-fractions-of-z]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
For every $x\in\mathbb{Q}$, there exist $m,n\in\mathbb{Z}$ with
$n\ne0_{\mathbb{Z}}$ such that
\[
x=\iota(m)\cdot\iota(n)^{-1}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Every rational number is a quotient of two embedded integers with nonzero
denominator.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:rational-class-notation]{Rational Class Notation}
  \item \hyperref[thm:every-nonzero-rational-has-multiplicative-inverse]{Every Nonzero Rational Has a Multiplicative Inverse}
  \item \hyperref[lem:vanishing-criterion-q]{Vanishing Criterion}
\end{itemize}
\end{dependencies}

\begin{corollarybox}{Corollary}
\begin{corollary}[The Embedding of $\mathbb{Z}$ into $\mathbb{Q}$ Is Not Surjective]
\label{cor:embedding-z-into-q-not-surjective}
The image of $\iota:\mathbb{Z}\to\mathbb{Q}$ is a proper subset of
\(\mathbb{Q}\).
\hyperref[prf:embedding-z-into-q-not-surjective]{\textit{Go to proof.}}
\end{corollary}
\end{corollarybox}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{Image}(\iota)\subsetneq\mathbb{Q}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Not every rational number is the image of an integer under the embedding.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[thm:density-of-q]{Density of $\mathbb{Q}$ in Itself}
  \item \hyperref[thm:embedding-z-into-q-preserves-order]{Embedding Preserves Order}
\end{itemize}
\end{dependencies}
