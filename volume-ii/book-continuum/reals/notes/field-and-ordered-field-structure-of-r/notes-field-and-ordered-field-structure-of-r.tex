\begin{theorembox}{Theorem}
\begin{theorem}[Nonzero $\mathbb{R}$ Is a Multiplicative Abelian Group]
\label{thm:r-nonzero-multiplicative-abelian-group}
The structure $(\mathbb{R}\setminus\{0_{\mathbb{R}}\},\cdot,1_{\mathbb{R}})$
is an abelian group.
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:two-sided-inverse]{Two-Sided Inverse}
  \item \hyperref[def:multiplication-on-r]{Multiplication on $\mathbb{R}$}
  \item \hyperref[thm:multiplication-on-r-associative]{Multiplication on $\mathbb{R}$ Is Associative}
  \item \hyperref[thm:multiplication-on-r-commutative]{Multiplication on $\mathbb{R}$ Is Commutative}
  \item \hyperref[thm:one-multiplicative-identity-on-r]{One Is a Multiplicative Identity on $\mathbb{R}$}
  \item \hyperref[thm:reciprocal-multiplicative-inverse-on-r]{Reciprocal Is a Multiplicative Inverse on $\mathbb{R}$}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[$\mathbb{R}$ Is a Field]
\label{thm:r-is-a-field}
The structure $(\mathbb{R},+,\cdot,0_{\mathbb{R}},1_{\mathbb{R}})$ is a field.
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[thm:r-additive-abelian-group]{$\mathbb{R}$ Is an Additive Abelian Group}
  \item \hyperref[thm:r-nonzero-multiplicative-abelian-group]{Nonzero $\mathbb{R}$ Is a Multiplicative Abelian Group}
  \item \hyperref[thm:distributivity-on-r]{Distributivity on $\mathbb{R}$}
  \item \hyperref[thm:zero-not-one-in-r]{Zero Is Not One in $\mathbb{R}$}
\end{itemize}
\end{dependencies}

\begin{corollarybox}{Corollary}
\begin{corollary}[Field Laws Hold on $\mathbb{R}$]
\label{cor:field-laws-on-r}
Every theorem proved generically for fields holds in $\mathbb{R}$, including
zero product, double negation, sign rules, cancellation, uniqueness of
inverses, inverse laws, and exponent laws.
\end{corollary}
\end{corollarybox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:two-sided-inverse]{Two-Sided Inverse}
\end{itemize}
\end{dependencies}

\subsection*{Order Compatibility}

\begin{theorembox}{Theorem}
\begin{theorem}[Addition Preserves Order on $\mathbb{R}$]
\label{thm:addition-preserves-order-on-r}
For all $\alpha,\beta,\gamma\in\mathbb{R}$,
\[
\alpha<\beta \Longrightarrow \alpha+\gamma<\beta+\gamma,
\]
and the corresponding non-strict statement also holds.
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:addition-on-r]{Addition on $\mathbb{R}$}
  \item \hyperref[def:order-on-r]{Order on $\mathbb{R}$}
  \item \hyperref[def:strict-order-on-r]{Strict Order on $\mathbb{R}$}
  \item \hyperref[thm:addition-preserves-order-on-q]{Addition Preserves Order on $\mathbb{Q}$}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[Multiplication by Positives Preserves Order on $\mathbb{R}$]
\label{thm:positive-multiplication-preserves-order-on-r}
For all $\alpha,\beta,\gamma\in\mathbb{R}$,
\[
0_{\mathbb{R}}<\gamma\land\alpha<\beta
\Longrightarrow
\gamma\cdot\alpha<\gamma\cdot\beta.
\]
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:multiplication-on-r]{Multiplication on $\mathbb{R}$}
  \item \hyperref[def:order-on-r]{Order on $\mathbb{R}$}
  \item \hyperref[def:strict-order-on-r]{Strict Order on $\mathbb{R}$}
  \item \hyperref[thm:positive-multiplication-preserves-order-on-q]{Positive Multiplication Preserves Order on $\mathbb{Q}$}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[$\mathbb{R}$ Is an Ordered Field]
\label{thm:r-is-an-ordered-field}
The structure $(\mathbb{R},+,\cdot,0_{\mathbb{R}},1_{\mathbb{R}},\le)$ is an
ordered field.
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[thm:r-is-a-field]{$\mathbb{R}$ Is a Field}
  \item \hyperref[thm:r-totally-ordered-set]{$\mathbb{R}$ Is a Totally Ordered Set}
  \item \hyperref[thm:addition-preserves-order-on-r]{Addition Preserves Order on $\mathbb{R}$}
  \item \hyperref[thm:positive-multiplication-preserves-order-on-r]{Multiplication by Positives Preserves Order on $\mathbb{R}$}
  \item \hyperref[def:trichotomy]{Trichotomy}
\end{itemize}
\end{dependencies}

\begin{corollarybox}{Corollary}
\begin{corollary}[Ordered-Field Laws Hold on $\mathbb{R}$]
\label{cor:ordered-field-laws-on-r}
Every theorem proved generically for ordered fields holds in $\mathbb{R}$.
\end{corollary}
\end{corollarybox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:strict-total-order]{Strict Total Order}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[$\mathbb{R}$ Is a Complete Ordered Field]
\label{thm:r-is-complete-ordered-field}
The ordered field $\mathbb{R}$ satisfies the least-upper-bound property.
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:real-ordered-field]{Complete Ordered Field}
  \item \hyperref[thm:r-is-an-ordered-field]{$\mathbb{R}$ Is an Ordered Field}
  \item \hyperref[thm:lub-property-r]{Least-Upper-Bound Property}
\end{itemize}
\end{dependencies}
