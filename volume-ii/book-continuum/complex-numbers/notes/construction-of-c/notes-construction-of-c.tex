\subsection*{Complex Prepairs}

\begin{topicbox}{The Last Number System Extension}
\begin{exposition}
The complex numbers extend the real numbers by adding a formal square root of
$-1$. Unlike the construction of $\mathbb{Z}$ from $\mathbb{W}$ or
$\mathbb{Q}$ from $\mathbb{Z}$, no nontrivial identification is needed:
ordered pairs of real numbers already have the correct equality. The same
construction pattern remains: representatives, an equivalence relation,
equivalence classes, then operations whose well-definedness is checked before
they are used.
\end{exposition}
\end{topicbox}

\begin{definitionbox}{Definition}
\begin{definition}[Complex Prepair]
\label{def:complex-prepair}
A \emph{complex prepair} is an ordered pair $(a,b)\in\mathbb{R}\times\mathbb{R}$,
read informally as the expression $a+bi$.
\end{definition}
\end{definitionbox}

\LeanFormalizes{def:complex-prepair}{lra-lean}{LRA.VolumeII.ComplexNumbers.Construction.Construction}{ComplexNumber}{pending}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{Pre}\mathbb{C}=\mathbb{R}\times\mathbb{R}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
A complex prepair is an ordered pair of real numbers, read as formal real and
imaginary coordinates.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:ordered-pair]{Ordered Pair}
  \item \hyperref[def:reals]{The Real Numbers}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition}
\begin{definition}[Complex Prepair Set]
\label{def:complex-prepair-set}
The complex prepair set is
\[
\operatorname{Pre}\mathbb{C}:=\mathbb{R}\times\mathbb{R}.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{Pre}\mathbb{C}:=\mathbb{R}\times\mathbb{R}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The pre-complex carrier is the Cartesian product of the real numbers with
itself.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:cartesian-product-rel]{Cartesian Product}
  \item \hyperref[def:reals]{The Real Numbers}
\end{itemize}
\end{dependencies}

\subsection*{Coordinate Equivalence}

\begin{definitionbox}{Definition}
\begin{definition}[Complex Coordinate Equivalence]
\label{def:complex-coordinate-equivalence}
For $(a,b),(c,d)\in\operatorname{Pre}\mathbb{C}$, define
\[
(a,b)\sim_{\mathbb{C}}(c,d)
\quad:\Longleftrightarrow\quad
a=c\ \text{ and }\ b=d.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
For all $(a,b),(c,d)\in\operatorname{Pre}\mathbb{C}$,
\[
(a,b)\sim_{\mathbb{C}}(c,d)
\Longleftrightarrow
a=c\land b=d.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
(a,b)\sim_{\mathbb{C}}(c,d)
\Longleftrightarrow
(a=c)\land(b=d).
\]
Thus the complex-number quotient is formally present, but the equivalence
classes are singleton coordinate-equality classes.
\end{remark*}

\begin{remark*}[Interpretation]
Complex coordinate equivalence identifies exactly those prepairs with equal
real coordinates and equal imaginary coordinates.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:complex-prepair-set]{Complex Prepair Set}
\end{itemize}
\end{dependencies}

\begin{lemmabox}{Lemma}
\begin{lemma}[Complex Coordinate Equivalence Is Reflexive]
\label{lem:complex-coordinate-equivalence-reflexive}
For every $(a,b)\in\operatorname{Pre}\mathbb{C}$,
\[
(a,b)\sim_{\mathbb{C}}(a,b).
\]
\hyperref[prf:complex-coordinate-equivalence-reflexive]{\textit{Go to proof.}}
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
For every $(a,b)\in\operatorname{Pre}\mathbb{C}$,
\[
(a,b)\sim_{\mathbb{C}}(a,b).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Every complex prepair is coordinate-equivalent to itself.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:complex-coordinate-equivalence]{Complex Coordinate Equivalence}
  \item \hyperref[ax:equality-reflexivity]{Equality Reflexivity}
\end{itemize}
\end{dependencies}

\begin{lemmabox}{Lemma}
\begin{lemma}[Complex Coordinate Equivalence Is Symmetric]
\label{lem:complex-coordinate-equivalence-symmetric}
For all prepairs $(a,b),(c,d)$,
\[
(a,b)\sim_{\mathbb{C}}(c,d)
\Longrightarrow
(c,d)\sim_{\mathbb{C}}(a,b).
\]
\hyperref[prf:complex-coordinate-equivalence-symmetric]{\textit{Go to proof.}}
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
For all $(a,b),(c,d)\in\operatorname{Pre}\mathbb{C}$,
\[
(a,b)\sim_{\mathbb{C}}(c,d)
\Longrightarrow
(c,d)\sim_{\mathbb{C}}(a,b).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Coordinate equivalence is symmetric because equality of real coordinates is
symmetric.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:complex-coordinate-equivalence]{Complex Coordinate Equivalence}
  \item \hyperref[thm:equality-symmetry]{Equality Symmetry}
\end{itemize}
\end{dependencies}

\begin{lemmabox}{Lemma}
\begin{lemma}[Complex Coordinate Equivalence Is Transitive]
\label{lem:complex-coordinate-equivalence-transitive}
For all prepairs $(a,b),(c,d),(e,f)$,
\[
(a,b)\sim_{\mathbb{C}}(c,d)\land
(c,d)\sim_{\mathbb{C}}(e,f)
\Longrightarrow
(a,b)\sim_{\mathbb{C}}(e,f).
\]
\hyperref[prf:complex-coordinate-equivalence-transitive]{\textit{Go to proof.}}
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
For all $(a,b),(c,d),(e,f)\in\operatorname{Pre}\mathbb{C}$,
\[
(a,b)\sim_{\mathbb{C}}(c,d)\land
(c,d)\sim_{\mathbb{C}}(e,f)
\Longrightarrow
(a,b)\sim_{\mathbb{C}}(e,f).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Coordinate equivalence is transitive because equality of real coordinates is
transitive in each coordinate.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:complex-coordinate-equivalence]{Complex Coordinate Equivalence}
  \item \hyperref[thm:equality-transitivity]{Equality Transitivity}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem}
\begin{theorem}[Complex Coordinate Equivalence Is an Equivalence Relation]
\label{thm:complex-coordinate-equivalence-relation}
The relation $\sim_{\mathbb{C}}$ is an equivalence relation on
\(\operatorname{Pre}\mathbb{C}\).
\hyperref[prf:complex-coordinate-equivalence-relation]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{EquivalenceRelation}(\operatorname{Pre}\mathbb{C},\sim_{\mathbb{C}}).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Reflexivity, symmetry, and transitivity are checked coordinate by coordinate
using equality in \(\mathbb{R}\). Thus this quotient construction has the same
formal shape as the constructions of \(\mathbb{Z}\) and \(\mathbb{Q}\), even
though the equivalence classes are singleton classes.
\end{remark*}

\begin{dependencies}
\begin{itemize}
\item \hyperref[def:complex-coordinate-equivalence]{Complex Coordinate Equivalence}
  \item \hyperref[lem:complex-coordinate-equivalence-reflexive]{Complex Coordinate Equivalence Is Reflexive}
  \item \hyperref[lem:complex-coordinate-equivalence-symmetric]{Complex Coordinate Equivalence Is Symmetric}
  \item \hyperref[lem:complex-coordinate-equivalence-transitive]{Complex Coordinate Equivalence Is Transitive}
\end{itemize}
\end{dependencies}

\subsection*{\texorpdfstring{Definition of $\mathbb{C}$}{Definition of C}}

\begin{definitionbox}{Definition}
\begin{definition}[Complex Numbers]
\label{def:complex-numbers}
The complex numbers are the quotient
\[
\mathbb{C}:=\operatorname{Pre}\mathbb{C}/{\sim_{\mathbb{C}}}.
\]
\end{definition}
\end{definitionbox}

\LeanFormalizes{def:complex-numbers}{lra-lean}{LRA.VolumeII.ComplexNumbers.Construction.Construction}{ComplexNumber}{pending}

\begin{remark*}[Standard quantified statement]
\[
\mathbb{C}=\operatorname{Quotient}(\operatorname{Pre}\mathbb{C},\sim_{\mathbb{C}}).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The complex numbers are the quotient of real coordinate prepairs by coordinate
equivalence.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:quotient-set]{Quotient Set}
  \item \hyperref[def:complex-prepair-set]{Complex Prepair Set}
  \item \hyperref[def:complex-coordinate-equivalence]{Complex Coordinate Equivalence}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition}
\begin{definition}[Complex Class Notation]
\label{def:complex-class-notation}
The notation $[a,b]_{\mathbb{C}}$ denotes the
\(\sim_{\mathbb{C}}\)-equivalence class of the prepair $(a,b)$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
For every $(a,b)\in\operatorname{Pre}\mathbb{C}$,
\[
[a,b]_{\mathbb{C}}=\{(c,d)\in\operatorname{Pre}\mathbb{C}:(c,d)\sim_{\mathbb{C}}(a,b)\}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Since coordinate equivalence is equality of coordinates, every complex number
has a unique representative
\[
[a,b]_{\mathbb{C}}.
\]
This same element is later written as $a+bi$.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:equivalence-class]{Equivalence Class}
  \item \hyperref[def:complex-coordinate-equivalence]{Complex Coordinate Equivalence}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition}
\begin{definition}[Zero, One, and the Imaginary Unit in $\mathbb{C}$]
\label{def:zero-one-i-in-c}
Define
\[
0_{\mathbb{C}}:=[0,0]_{\mathbb{C}},
\qquad
1_{\mathbb{C}}:=[1,0]_{\mathbb{C}},
\qquad
i:=[0,1]_{\mathbb{C}}.
\]
\end{definition}
\end{definitionbox}

\LeanFormalizes{def:zero-one-i-in-c}{lra-lean}{LRA.VolumeII.ComplexNumbers.Construction.Construction}{imaginary_unit}{pending}

\begin{remark*}[Standard quantified statement]
\[
0_{\mathbb{C}}=[0,0]_{\mathbb{C}},
\qquad
1_{\mathbb{C}}=[1,0]_{\mathbb{C}},
\qquad
i=[0,1]_{\mathbb{C}}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The distinguished complex constants are the coordinate classes for zero, one,
and the imaginary unit.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:equiv-class]{Equivalence Class}
  \item \hyperref[def:complex-class-notation]{Complex Class Notation}
  \item \hyperref[def:reals]{The Real Numbers}
\end{itemize}
\end{dependencies}
