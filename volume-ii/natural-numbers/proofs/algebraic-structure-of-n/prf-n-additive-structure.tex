\newpage
\phantomsection
\label{prf:n-additive-structure}
\LRAProofFor{thm:n-additive-structure}

\begin{remark*}[Return]
\hyperref[thm:n-additive-structure]{Return to Theorem}
\end{remark*}

\begin{theorem*}[$N$ Is Closed, Associative, and Commutative Under Addition]
Let $(P,S,1)$ be a Peano system. The binary operation $+$ on $P$ satisfies:
\begin{enumerate}
  \item \textbf{Closure.} For all $a,b \in P$, $a+b \in P$.
  \item \textbf{Associativity.} For all $a,b,c \in P$,
  $(a+b)+c = a+(b+c)$.
  \item \textbf{Commutativity.} For all $a,b \in P$, $a+b = b+a$.
  \item \textbf{Left cancellation.} For all $a,b,c \in P$, if $a+b = a+c$ then $b=c$.
  \item \textbf{Right cancellation.} For all $a,b,c \in P$, if $b+a = c+a$ then $b=c$.
\end{enumerate}
\end{theorem*}

\begin{proof}[Professional Standard Proof]
\LRAProofBodyStart
TODO: professional standard proof for n-additive-structure.
\end{proof}

\begin{proof}[Detailed Learning Proof]
\LRAProofBodyStart
TODO: detailed learning proof for n-additive-structure.
\end{proof}

\begin{remark*}[Proof structure]
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[thm:addition-associative]{Addition Is Associative}
  \item \hyperref[thm:addition-commutative]{Addition Is Commutative}
  \item \hyperref[thm:addition-left-cancellation]{Left Cancellation for Addition}
  \item \hyperref[thm:addition-right-cancellation]{Right Cancellation for Addition}
\end{itemize}
\end{dependencies}

\clearpage
