\subsection*{The Extended Multiplication Operation}

\begin{tcolorbox}[colback=defbox, colframe=defborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Definition (Multiplication on \texorpdfstring{$\mathbb{W}$}{W})}},
  fonttitle=\small\bfseries]
\begin{definition}[Multiplication on $\mathbb{W}$]
\label{def:multiplication-on-w}
Define $\cdot_{\mathbb{W}}:\mathbb{W}\times\mathbb{W}\to\mathbb{W}$ by
\[
0\cdot_{\mathbb{W}}a=0,\qquad
a\cdot_{\mathbb{W}}0=0,
\]
and, for $a,b\in\mathbb{N}$,
\[
a\cdot_{\mathbb{W}}b=a\cdot_{\mathbb{N}}b.
\]
\end{definition}
\end{tcolorbox}

\begin{remark*}[Interpretation]
The zero cases are new. The positive-positive case is inherited from
$\mathbb{N}$. Since $0\notin\mathbb{N}$, these clauses do not compete.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:whole-numbers]{Whole Numbers}
  \item \hyperref[def:multiplication-on-peano-system]{Multiplication on a Peano System}
\end{itemize}
\end{dependencies}

\begin{tcolorbox}[colback=thmbox, colframe=thmborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Theorem (Multiplication on \texorpdfstring{$\mathbb{W}$}{W} Is Well-Defined)}},
  fonttitle=\small\bfseries]
\begin{theorem}[Multiplication on $\mathbb{W}$ Is Well-Defined]
\label{thm:multiplication-on-w-well-defined}
The case definition of $\cdot_{\mathbb{W}}$ determines a binary operation on
$\mathbb{W}$.
\hyperref[prf:multiplication-on-w-well-defined]{Go to proof.}
\end{theorem}
\end{tcolorbox}

\begin{remark*}[Interpretation]
The inherited positive case is already well-defined on $\mathbb{N}$, and the
new zero cases land in the distinguished element $0\in\mathbb{W}$.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:multiplication-on-w]{Multiplication on $\mathbb{W}$}
  \item \hyperref[thm:multiplication-well-defined-on-peano-system]{Multiplication Is Well-Defined on a Peano System}
\end{itemize}
\end{dependencies}

\begin{tcolorbox}[colback=thmbox, colframe=thmborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Theorem (Zero Is Absorbing for Multiplication on \texorpdfstring{$\mathbb{W}$}{W})}},
  fonttitle=\small\bfseries]
\begin{theorem}[Zero Is Absorbing for Multiplication on $\mathbb{W}$]
\label{thm:zero-absorbing-for-multiplication-on-w}
For every $a\in\mathbb{W}$,
\[
0\cdot_{\mathbb{W}}a=0
\qquad\text{and}\qquad
a\cdot_{\mathbb{W}}0=0.
\]
\hyperref[prf:zero-absorbing-for-multiplication-on-w]{Go to proof.}
\end{theorem}
\end{tcolorbox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:multiplication-on-w]{Multiplication on $\mathbb{W}$}
\end{itemize}
\end{dependencies}

\begin{tcolorbox}[colback=thmbox, colframe=thmborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Theorem (One Is a Multiplicative Identity on \texorpdfstring{$\mathbb{W}$}{W})}},
  fonttitle=\small\bfseries]
\begin{theorem}[One Is a Multiplicative Identity on $\mathbb{W}$]
\label{thm:one-multiplicative-identity-on-w}
For every $a\in\mathbb{W}$,
\[
1\cdot_{\mathbb{W}}a=a
\qquad\text{and}\qquad
a\cdot_{\mathbb{W}}1=a.
\]
\hyperref[prf:one-multiplicative-identity-on-w]{Go to proof.}
\end{theorem}
\end{tcolorbox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:multiplication-on-w]{Multiplication on $\mathbb{W}$}
  \item \hyperref[thm:multiplication-with-one]{Multiplication With $1$}
  \item \hyperref[thm:one-times-n]{One Times $n$}
\end{itemize}
\end{dependencies}

\begin{tcolorbox}[colback=thmbox, colframe=thmborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Theorem (Multiplication on \texorpdfstring{$\mathbb{W}$}{W} Extends Multiplication on \texorpdfstring{$\mathbb{N}$}{N})}},
  fonttitle=\small\bfseries]
\begin{theorem}[Multiplication on $\mathbb{W}$ Extends Multiplication on $\mathbb{N}$]
\label{thm:multiplication-on-w-extends-multiplication-on-n}
For all $a,b\in\mathbb{N}$,
\[
a\cdot_{\mathbb{W}}b=a\cdot_{\mathbb{N}}b.
\]
\hyperref[prf:multiplication-on-w-extends-multiplication-on-n]{Go to proof.}
\end{theorem}
\end{tcolorbox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:multiplication-on-w]{Multiplication on $\mathbb{W}$}
  \item \hyperref[def:multiplication-on-peano-system]{Multiplication on a Peano System}
\end{itemize}
\end{dependencies}

\begin{tcolorbox}[colback=thmbox, colframe=thmborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Theorem (Multiplication on \texorpdfstring{$\mathbb{W}$}{W} Is Associative)}},
  fonttitle=\small\bfseries]
\begin{theorem}[Multiplication on $\mathbb{W}$ Is Associative]
\label{thm:multiplication-on-w-associative}
For all $a,b,c\in\mathbb{W}$,
\[
(a\cdot_{\mathbb{W}}b)\cdot_{\mathbb{W}}c
=
a\cdot_{\mathbb{W}}(b\cdot_{\mathbb{W}}c).
\]
\hyperref[prf:multiplication-on-w-associative]{Go to proof.}
\end{theorem}
\end{tcolorbox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[thm:zero-absorbing-for-multiplication-on-w]{Zero Is Absorbing for Multiplication on $\mathbb{W}$}
  \item \hyperref[thm:multiplication-associative]{Multiplication Is Associative}
\end{itemize}
\end{dependencies}

\begin{tcolorbox}[colback=thmbox, colframe=thmborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Theorem (Multiplication on \texorpdfstring{$\mathbb{W}$}{W} Is Commutative)}},
  fonttitle=\small\bfseries]
\begin{theorem}[Multiplication on $\mathbb{W}$ Is Commutative]
\label{thm:multiplication-on-w-commutative}
For all $a,b\in\mathbb{W}$,
\[
a\cdot_{\mathbb{W}}b=b\cdot_{\mathbb{W}}a.
\]
\hyperref[prf:multiplication-on-w-commutative]{Go to proof.}
\end{theorem}
\end{tcolorbox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:multiplication-on-w]{Multiplication on $\mathbb{W}$}
  \item \hyperref[thm:multiplication-commutative]{Multiplication Is Commutative}
\end{itemize}
\end{dependencies}

\begin{tcolorbox}[colback=thmbox, colframe=thmborder, arc=2pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  title={\small\textbf{Theorem (Multiplication Distributes over Addition on \texorpdfstring{$\mathbb{W}$}{W})}},
  fonttitle=\small\bfseries]
\begin{theorem}[Multiplication Distributes over Addition on $\mathbb{W}$]
\label{thm:multiplication-distributes-over-addition-on-w}
For all $a,b,c\in\mathbb{W}$,
\[
a\cdot_{\mathbb{W}}(b+_{\mathbb{W}}c)
=
(a\cdot_{\mathbb{W}}b)+_{\mathbb{W}}(a\cdot_{\mathbb{W}}c).
\]
\hyperref[prf:multiplication-distributes-over-addition-on-w]{Go to proof.}
\end{theorem}
\end{tcolorbox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:addition-on-w]{Addition on $\mathbb{W}$}
  \item \hyperref[def:multiplication-on-w]{Multiplication on $\mathbb{W}$}
  \item \hyperref[thm:multiplication-distributes-over-addition]{Multiplication Distributes Over Addition}
\end{itemize}
\end{dependencies}
